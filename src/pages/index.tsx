import React from 'react';

import { Header, Main } from 'components';

const Home: React.FC = () => {
  return (
    <React.Fragment>
      <Header />
      <Main />
    </React.Fragment>
  );
};

export default Home;
